# Admin FE environment example for local development
# Vite will expose variables prefixed with VITE_ to client code.
# Copy to .env and adjust as needed.

# Backend API base URL for admin-fe
VITE_API_BASE=http://127.0.0.1:9292

# Optional: start date to show the green "新" badge for 30 days (relative logic)
# Format: YYYY-MM-DD
# If unset, the app will use the current build/launch date as the start.
VITE_STORE_FINANCE_NEW_BADGE_START=2026-01-04
